Reminder
- The script at .flow-os/bin/flow-go launches a sub-shell. 
- Therefore, go must be implemented as a shell function instead.
- The flow-go command is implemented as a method in .flow-os/shell/zsh/functions.
- When listing `.flow-os/bin` commands, 
  this placeholder file ensures `flow-go` appears. 
